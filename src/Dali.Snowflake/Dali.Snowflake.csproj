<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Snowflake ID generator for Dali — wraps Aero.Core's Snowflake.NewId() for unique, time-sortable long primary keys.</Description>
    <PackageTags>surrealdb dali snowflake idgen</PackageTags>
    <!-- NU1903: Newtonsoft.Json 10.0.2 vulnerability is transitive through Aero.Core → WindowsAzure.Storage.
         We don't use Newtonsoft.Json or WindowsAzure.Storage directly. -->
    <NoWarn>$(NoWarn);NU1903</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Dali\Dali.csproj" />
    <PackageReference Include="Aero.Core" />
  </ItemGroup>

</Project>
